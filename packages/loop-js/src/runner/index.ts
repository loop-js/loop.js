/**
 * runner/ — the connection-facing shell: one Run inside one Sandbox, serving/dialing the
 * event stream. Kept unmixed with engine core so the wire boundary stays clean. Build order
 * step 3+. Deliberately kept as an empty placeholder (owner decision, 2026-07-09) — do not
 * flag for deletion in architecture reviews.
 */

export {}
